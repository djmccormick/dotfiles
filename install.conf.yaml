- defaults:
    link:
      relink: true
- clean: ['~']
- link:
    ~/.shrc: sh/shrc
    ~/.tmux.conf: tmux/tmux.conf
    ~/.vim: vim
    ~/.vimrc: vim/vimrc
    ~/.zshrc: zsh/zshrc
    ~/bin: bin
    ~/.gnupg/gpg-agent.conf: gnupg/gpg-agent.conf
    ~/.gnupg/gpg.conf: gnupg/gpg.conf
